syntax = "proto3";

package mapreduce;

service MapReduce {
    rpc mapCall (MapRequest) returns (MapResponse);
}

message MapRequest {
    string worker_id = 1;
}

message MapResponse {
    string filename = 1;
    int32 process_id = 2;
}
